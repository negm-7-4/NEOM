# Verification scripts

Development tools used to check this build. Not part of the application and
not bundled — `.verify/` is excluded in `eslint.config.mjs` and contains no
app code.

See the "Verification" section of the project README for what each script
covers and how to run them.

Generated output (`shots/`, `captured-email.*`) is gitignored.
